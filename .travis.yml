#language: java
#
#jdk:
#  - oraclejdk8
#  - openjdk7
#
#before_install:
#  - cd java

language: java
jdk:
  - oraclejdk8
sudo: false
script: mvn clean verify


before_install:
   - if [ "$TRAVIS_OS_NAME" == "osx" ]; then export
    JAVA_HOME=$(/usr/libexec/java_home); fi

